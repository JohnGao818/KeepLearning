#include <stdio.h>
#include <windows.h>
#include <string.h>
#define MAXSIZE 100

typedef struct {
    int data[MAXSIZE];
    int length;
}SqList;

int Binay_Search(SqList sq, int key) {
    int p = 1, q = sq.length;
    int mid;
    while (p <= q) {
        mid = (p+q)/2;
        if (sq.) {

        }
    }
}

int main()
{
    int A[10] = {0,5, 6, 9, 1, 3, 7, 8, 4, 2};
    system("pause");
}
